<Page x:Class="BKFluentChat.Views.Pages.TestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BKFluentChat.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      d:DataContext="{d:DesignInstance Type=local:TestPage}"
      xmlns:models="clr-namespace:BKFluentChat.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestPage">


    <Page.Resources>
        <!-- 资源 1: 联系人条目模板 -->
        <DataTemplate x:Key="ContactItemTemplate" DataType="{x:Type models:Contact}">
            <Button Command="{Binding ViewModel.SelectItemCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                    CommandParameter="{Binding}"
                    Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch"
                    Padding="0" Margin="0" >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Grid Margin="12,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <!-- 左侧列，宽度自适应头像 -->
                        <ColumnDefinition Width="*"/>
                        <!-- 右侧列，占据剩余所有空间 -->
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：头像 -->
                    <Ellipse Grid.Column="0" Width="40" Height="40" VerticalAlignment="Center">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue={StaticResource DefaultAvatar}}"/>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- 右侧：两行文字 -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Name}" FontSize="14" TextTrimming="CharacterEllipsis"/>
                        <!-- 第一行：备注名 -->
                        <TextBlock Text="{Binding Remark}" FontSize="14" TextTrimming="CharacterEllipsis"/>
                        <!-- 第二行：状态 -->
                        <TextBlock Text="{Binding Status}" Foreground="Gray" FontSize="12" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Grid>
            </Button>
        </DataTemplate>

        <!-- 资源 2: Expander 样式 -->
        <Style x:Key="MinimalExpanderStyle" TargetType="Expander">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="HeaderToggleButton" Grid.Row="0"
                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Command="{Binding ViewModel.SelectItemCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                          CommandParameter="{Binding}">

                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent" Padding="5">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="▶" x:Name="Arrow" Margin="5,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ToggleButton>
                            <ContentPresenter Grid.Row="1" x:Name="Content" Visibility="Collapsed" Margin="15,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- 保持 IsExpanded 触发器不变 -->
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="Arrow" Property="Text" Value="▼"/>
                                <Setter TargetName="Content" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- 当鼠标悬停在 Expander (的头部) 上时，改变背景色 -->
                                <Setter Property="Background" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧列表 -->
        <ui:ListView Grid.Column="0" ItemsSource="{Binding ViewModel.ContactGroups}"
                  Background="Transparent" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ui:ListView.ItemContainerStyle>
                <Style TargetType="ui:ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ui:ListViewItem">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>

                </Style>
            </ui:ListView.ItemContainerStyle>
            <ui:ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:ContactGroup}">
                    <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                              Style="{StaticResource MinimalExpanderStyle}">
                        <ItemsControl ItemsSource="{Binding Contacts}"
                                      ItemTemplate="{StaticResource ContactItemTemplate}"/>
                    </Expander>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>

        <!-- 右侧详细信息面板 -->
        <Grid Grid.Column="1" Background="#3F3F3F">
            <!--<TextBlock Text="{Binding ViewModel.SelectedItemName}"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        -->
            <!-- 默认情况下，我们让它可见，这样更容易调试 -->
            <!--
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewModel.SelectedItemName}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>-->
            <!-- 完整的右侧详细信息视图 (ScrollViewer 版本) -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewModel.SelectedContact}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>

                <StackPanel DataContext="{Binding ViewModel.SelectedContact}" Margin="30">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0" Width="80" Height="80">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding AvatarUrl, TargetNullValue={StaticResource DefaultAvatar}}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding QqNumber}" Foreground="Gray" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding Status}" Foreground="Gray" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Top" HorizontalAlignment="Right">
                                <ui:SymbolIcon Symbol="ThumbLike24" FontSize="20"/>
                                <TextBlock Text="{Binding LikesCount}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>

                        <!-- 个人信息行 -->
                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" TextElement.Foreground="Gray">
                            <ui:SymbolIcon Symbol="AppGeneric24" Foreground="#6495ED"/>
                            <TextBlock Text="{Binding Gender}" Margin="5,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Age, StringFormat={}{0}岁}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding BirthDate}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ZodiacSign}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- 阳光/月亮/星星 -->
                        <StackPanel Orientation="Horizontal" Margin="0,15,0,20">
                            <ui:SymbolIcon Symbol="WeatherSunny24" Foreground="Orange"/>
                            <ui:SymbolIcon Symbol="WeatherSunny24" Foreground="Orange"/>
                            <ui:SymbolIcon Symbol="WeatherMoon24" Foreground="Silver"/>
                            <ui:SymbolIcon Symbol="WeatherMoon24" Foreground="Silver"/>
                            <ui:SymbolIcon Symbol="Star24" Foreground="Gold"/>
                        </StackPanel>

                        <Separator/>

                        <!-- 详细信息Grid (修正了重叠问题) -->
                        <Grid Margin="0,20,0,0" TextElement.Foreground="Gray">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Row 0: 备注 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="Edit24"/>
                                <TextBlock Text="备注" Margin="5,0,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Remark}" Foreground="White" Margin="20,0,0,0" VerticalAlignment="Center"/>

                            <!-- Row 1: 好友分组 -->
                            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,0,0">
                                <ui:SymbolIcon Symbol="Group24"/>
                                <TextBlock Text="好友分组" Margin="5,0,0,0"/>
                            </StackPanel>
                            <ComboBox Grid.Row="1" Grid.Column="1" HorizontalContentAlignment="Right" SelectedItem="{Binding GroupName}" Margin="20,10,0,0" Width="150"/>

                            <!-- Row 2: 签名 -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,0,0">
                                <ui:SymbolIcon Symbol="Pen24"/>
                                <TextBlock Text="签名" Margin="5,0,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Signature}" Foreground="White" Margin="20,15,0,0" VerticalAlignment="Center"/>

                            <!-- Row 3: QQ空间 -->
                            <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,0,0">
                                <ui:SymbolIcon Symbol="Globe24"/>
                                <TextBlock Text="QQ空间" Margin="5,0,0,0"/>
                            </StackPanel>
                            <ui:SymbolIcon Grid.Row="3" Grid.Column="1" Symbol="ChevronRight24" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,15,0,0"/>

                            <!-- Row 4: 精选照片 -->
                            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,15,0,0">
                                <ui:SymbolIcon Symbol="Apps24"/>
                                <TextBlock Text="精选照片" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Grid>

                        <!-- 照片墙 -->
                        <ItemsControl ItemsSource="{Binding FeaturedPhotos}" Margin="0,10,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1" Columns="6"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="5" Margin="3">
                                        <Image Source="{Binding}" Stretch="UniformToFill"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- 底部按钮 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                            <Button Content="分享"/>
                            <Button Content="音视频通话" Margin="10,0"/>
                            <Button Content="发消息" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

        </Grid>
    </Grid>
</Page>
